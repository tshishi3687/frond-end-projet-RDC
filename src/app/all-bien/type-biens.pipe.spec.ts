import { TypeBiensPipe } from './type-biens.pipe';

describe('TypeBiensPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeBiensPipe();
    expect(pipe).toBeTruthy();
  });
});
