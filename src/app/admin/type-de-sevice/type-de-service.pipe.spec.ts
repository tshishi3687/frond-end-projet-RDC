import { TypeDeServicePipe } from './type-de-service.pipe';

describe('TypeDeServicePipe', () => {
  it('create an instance', () => {
    const pipe = new TypeDeServicePipe();
    expect(pipe).toBeTruthy();
  });
});
