import { TypeDeBienPipe } from './type-de-bien.pipe';

describe('TypeDeBienPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeDeBienPipe();
    expect(pipe).toBeTruthy();
  });
});
